<style media="screen">
  #loading {
    position: fixed;
    display: block;
    width: 100%;
    height: 100%;
    top: 0;
    left: 0;
    text-align: center;
    background-color: #fff;
    z-index: 99;
  }

  #loading-image {
    position: absolute;
    top: 0;
    bottom: 0;
    margin: auto;
    z-index: 100;
  }

  /* TODO LIST BEGIN */
  .todo {
    min-height:75px;
    max-height:500px;
    overflow:auto;
  }
  .todo ul {
    padding: 0px;
  }
  .todo ul li {
    display:flex;
    list-style:none;
    border-bottom: 1px solid #34373d;
    padding: 15px;
  }
  .todo ul li:hover {
    background-color: #34373d;
  }
  .todo .todo-content-right {
    margin-left:auto;
  }
  .todo-add {
    margin-left:auto;
  }
  /* TODO LIST END */

  /* Client Page Begin*/
  button.clientButton::before {
    font-family: fontAwesome;
    content: "\f040";
  }
  #client-form {
    position: fixed;
    top: 70px;
    left: 0;
    width: 80%;
    right: 0;
    margin: 0 auto;
  }
  .client-form-content {
    width: 100%;
  }
  .overlay {
    position: fixed;
    top:0;
    left:0;
    width:100%;
    height:100%;
  }
  .overlay-dark {
    background-color: rgba(10,10,10,0.8);
    backdrop-filter: blur(6px);
    z-index: 10;
  }
  .overlay .modal-body {
    max-height:350px;
    overflow:auto;
  }
  #client-form-wrapper {
    display:none;
  }
  #intraday p:first-child {
    -webkit-animation: blink-5 infinite both;
    animation: blink-5 infinite both;
    animation-duration: 5s;
  }
  /* CLient Page End*/

  .block-content::-webkit-scrollbar-track
  {
    -webkit-box-shadow: inset 0 0 6px rgba(0,0,0,0.3);
    background-color: #555;
  }

  .block-content::-webkit-scrollbar
  {
    width: 12px;
    background-color: #F5F5F5;
  }

  .block-content::-webkit-scrollbar-thumb
  {
    -webkit-box-shadow: inset 0 0 6px rgba(0,0,0,.3);
     background-color: #999;
  }
  .canvasjs-chart-credit {
    display: none !important;
  }
  #canvasTrialHide{
    height: 10px;
    width: 80px;
    position: absolute;
    top: 0;
    background-color:#2d3035;
  }
  table {
    border-collapse: separate; /* Don't collapse */
  }
  table,th,td {
    border:0;
    font-size:0.9em;
  }
  th {
    position: sticky;
    top: 0;
    background: #2d3035;
    border-bottom: 1px solid #e1e1e1;
  }
  th,td {
    border-color: rgba(255, 255, 255, 0.12);
    border-bottom: 1px solid #d5dfea;
    border-top: 0;
    padding: 10px;
    border-left: 0;
    color: #e1e1e1;
  }
  .number-mid {
    text-align: center;
  }
  .number-right {
    text-align: right;
    padding-right: 20px;
  }
  .alert>.start-icon {
      margin-right: 0;
      min-width: 20px;
      text-align: center;
  }

  .alert>.start-icon {
      margin-right: 5px;
  }

  .greencross
  {
    font-size:18px;
        color: #25ff0b;
      text-shadow: none;
  }

  .alert-simple.alert-success
  {
    border: 1px solid rgba(36, 241, 6, 0.46);
      background-color: rgba(7, 149, 66, 0.12156862745098039);
      box-shadow: 0px 0px 2px #259c08;
      color: #0ad406;
    text-shadow: 2px 1px #00040a;
    transition:0.5s;
    cursor:pointer;
  }
  .alert-success:hover{
    background-color: rgba(7, 149, 66, 0.35);
    transition:0.5s;
  }
  .ddk-noti-container {
    display: none;
    position: fixed;
    right: 0;
    padding: 10px;
    background: #2d3035;
    border-left: 1px solid #34373d;
    box-shadow: 0px 0px 25px #1111118a;
    width: 450px;
    height: 100%;
    top: 0;
    z-index: 98;
  }
  .ddk-noti-header {
    height: 50px;
  }
  .ddk-noti-body {
    max-height: 80%;
    overflow: auto;
  }
  .ddk-noti-close {
    position: absolute;
    right: 10px;
  }
  #ddk-noti {
    cursor: pointer;
  }
  .tooltips {
    cursor: pointer;
  }
  .tooltips .tooltiptext {
    visibility: hidden;
    width: 250px;
    background-color: black !important;
    color: #fff;
    text-align: left;
    border-radius: 6px;
    padding: 15px;
    font-size: 0.8em;

    /* Position the tooltip */
    position: absolute;
    top: 20px;
    right: 5px;
    z-index: 1;
  }

  .tooltips th {
    background-color: black !important;
  }

  .tooltips:hover .tooltiptext {
    visibility: visible;
  }

  #welcomeModal {
    position: fixed;
    z-index: 999;
    padding-top: 200px;
    left: 0;
    top: 0;
    width: 100%;
    height: 100%;
    overflow: auto;
    background: url('img/copytradeWelcome.jpg');
    background-size: cover;
  }
  #welcomeModal .modal-content{
    background-color: #2d3035de !important;
  }
  #welcomeModal .modal-header{
    background: none !important;
  }
  #welcomeModal .modal-footer{
    background: none !important;
  }
  #welcomeModal-msg {
    font-size: 2em;
    color: #fff;
  }
  .welcome-content {
    animation-name: bounceIn;
    animation-duration: 450ms;
    animation-timing-function: linear;
    animation-fill-mode: forwards;
  }
  .alert-simple.alert-danger
  {
  border: 1px solid rgba(241, 6, 6, 0.81);
    background-color: rgba(220, 17, 1, 0.16);
    box-shadow: 0px 0px 2px #ff0303;
    color: #ff0303;
    text-shadow: 2px 1px #00040a;
    transition:0.5s;
    cursor:pointer;
  }

  .alert-danger:hover
  {
     background-color: rgba(220, 17, 1, 0.33);
     transition:0.5s;
  }

  .danger
  {
      font-size: 18px;
    color: #ff0303;
    text-shadow: none;
  }
  .alert:before {
    content: '';
    position: absolute;
    width: 0;
    height: calc(100% - 44px);
    border-left: 1px solid;
    border-right: 2px solid;
    border-bottom-right-radius: 3px;
    border-top-right-radius: 3px;
    left: 0;
    top: 50%;
    transform: translate(0,-50%);
      height: 20px;
  }

  /**
 * ----------------------------------------
 * animation blink-1
 * ----------------------------------------
 */
 @-webkit-keyframes blink-1 {
   0%,
   100% {
     border: 1px solid var(--canhbaoCol1);
     background-color: var(--canhbaoCol11);
     box-shadow: 0px 0px 2px var(--canhbaoCol1);
     color: var(--canhbaoCol1);
     opacity: 1;
   }
   50% {
     border: 1px solid var(--canhbaoCol2);
     background-color: var(--canhbaoCol21);
     box-shadow: 0px 0px 2px var(--canhbaoCol2);
     color: var(--canhbaoCol2);
     opacity: 1;
   }
   25%,75% {
     opacity: 0.5;
   }
 }
 @keyframes blink-1 {
   0%,
   100% {
     border: 1px solid var(--canhbaoCol1);
     background-color: var(--canhbaoCol11);
     box-shadow: 0px 0px 2px var(--canhbaoCol1);
     color: var(--canhbaoCol1);
     opacity: 1;
   }
   50% {
     border: 1px solid var(--canhbaoCol2);
     background-color: var(--canhbaoCol21);
     box-shadow: 0px 0px 2px var(--canhbaoCol2);
     color: var(--canhbaoCol2);
     opacity: 1;
   }
   25%,75% {
     opacity: 0.5;
   }
 }

  @keyframes bounceIn{
    0%{
      opacity: 0;
      transform: scale(0.3) translate3d(0,0,0);
    }
    50%{
      opacity: 0.9;
      transform: scale(1.1);
    }
    80%{
      opacity: 1;
      transform: scale(0.89);
    }
    100%{
      opacity: 1;
      transform: scale(1) translate3d(0,0,0);
    }
  }
</style>
